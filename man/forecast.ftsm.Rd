% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast.ftsm.R, R/forecast.ftsm_temp.R
\name{forecast.ftsm}
\alias{forecast.ftsm}
\title{Forecast a functional time series model}
\usage{
\method{forecast}{ftsm}(
  object,
  h = 10,
  method = c("ets", "arima", "ar", "ets.na", "rwdrift", "rw", "struct", "arfima",
    "ridge2f"),
  level = 95,
  jumpchoice = c("fit", "actual"),
  pimethod = c("parametric", "nonparametric"),
  B = 100,
  usedata = nrow(object$coeff),
  adjust = TRUE,
  model = NULL,
  damped = NULL,
  stationary = FALSE,
  FUN = NULL,
  FUN_method = c("block-bootstrap", "surrogate", "kde", "bootstrap", "fitdistr"),
  FUN_nsim = B,
  FUN_block_size = 5,
  FUN_seed = 123L,
  ...
)

\method{forecast}{ftsm}(
  object,
  h = 10,
  method = c("ets", "arima", "ar", "ets.na", "rwdrift", "rw", "struct", "arfima",
    "ridge2f"),
  level = 95,
  jumpchoice = c("fit", "actual"),
  pimethod = c("parametric", "nonparametric"),
  B = 100,
  usedata = nrow(object$coeff),
  adjust = TRUE,
  model = NULL,
  damped = NULL,
  stationary = FALSE,
  FUN = NULL,
  FUN_method = c("block-bootstrap", "surrogate", "kde", "bootstrap", "fitdistr"),
  FUN_nsim = B,
  FUN_block_size = 5,
  FUN_seed = 123L,
  ...
)
}
\arguments{
\item{object}{An object of class \code{ftsm} returned by \code{ftsm()}.}

\item{h}{Forecast horizon (number of periods to forecast).}

\item{method}{Forecasting method: "ets" (default), "arima", "ar", "ets.na", "rwdrift", "rw", "struct", or "arfima".}

\item{level}{Confidence level for prediction intervals (default: 80).}

\item{jumpchoice}{Method for handling the jump-off point: "fit" (default) or "actual".}

\item{pimethod}{Method for prediction intervals: "parametric" (default) or "nonparametric".}

\item{B}{Number of bootstrap replications for nonparametric prediction intervals (default: 100).}

\item{usedata}{Number of observations to use for fitting (default: all available).}

\item{adjust}{Logical. If TRUE, adjust forecasts for bias (default: TRUE).}

\item{model}{ETS model specification (optional).}

\item{damped}{Logical. If TRUE, use damped trend (optional).}

\item{stationary}{Logical. If TRUE, force stationarity (default: FALSE).}

\item{FUN}{forecasting function (if provided, \code{method} is ignored)}

\item{FUN_method}{Method to be used for conformalization (simulation of calibrated residuals)
when \code{FUN} is not \code{NULL}.}

\item{FUN_nsim}{Number of simulations when \code{FUN} is not \code{NULL}.}

\item{FUN_block_size}{Block size for block-bootstrap when \code{FUN} is not \code{NULL}.}

\item{FUN_seed}{Seed for reproducibility when \code{FUN} is not \code{NULL}.}

\item{...}{Additional arguments passed to the forecasting method.}
}
\value{
An object of class \code{ftsf} containing:
\item{mean}{Point forecasts as a functional time series object}
\item{lower}{Lower prediction bounds (parametric method)}
\item{upper}{Upper prediction bounds (parametric method)}
\item{fitted}{One-step fitted values}
\item{error}{One-step forecast errors}
\item{coeff}{Forecasts and prediction intervals for each coefficient}
\item{coeff.error}{Errors in coefficient estimation}
\item{var}{Variance components}
\item{model}{The original \code{ftsm} object}
\item{bootsamp}{Bootstrap samples (nonparametric method)}

An object of class \code{ftsf} containing:
\item{method}{Forecasting method used}
\item{x}{Time points}
\item{y}{Original functional time series}
\item{fitted}{Fitted values}
\item{residuals}{Residuals}
\item{mean}{Point forecasts}
\item{lower}{Lower prediction intervals}
\item{upper}{Upper prediction intervals}
\item{level}{Confidence level}
\item{xname}{Name of x variable}
\item{yname}{Name of y variable}
}
\description{
Produces forecasts from a functional time series model using various
univariate time series forecasting methods applied to the coefficients.

Forecasts a functional time series model using various univariate time series methods.
}
\details{
This function forecasts a functional time series by applying univariate time series
forecasting methods to the coefficients obtained from a functional principal
component analysis. The forecasts are then transformed back to functional space.

For the \code{method = "ets"} option, the \code{model} parameter can be specified as:
\itemize{
  \item{A single character string: Applied to all coefficients except the first}
  \item{A vector of length \code{nb-1}: Applied to coefficients 2 through nb}
  \item{A vector of length \code{nb}: Applied to all coefficients}
}
The first coefficient (mean function) always uses \code{"ANN"} model unless specified otherwise.
}
\examples{
\dontrun{
# Fit functional time series model
fmodel <- ftsm(y = fts(data))

# Forecast using ETS method
forecast_ets <- forecast(fmodel, h = 10, method = "ets")

# Forecast using ARIMA method
forecast_arima <- forecast(fmodel, h = 10, method = "arima")

# Forecast with custom function
custom_forecast <- forecast(fmodel, h = 10, FUN = forecast::thetaf)

# Plot forecasts
plot(forecast_ets)
}

\dontrun{
# Fit functional time series model
fit <- ftsm(pm_10_GR, order = 3)
# Forecast 12 periods ahead
fc <- forecast(fit, h = 12, method = "ets")
# Plot forecasts
plot(fc)

fit <- ftsm(pm_10_GR, order = 3, mean=FALSE); fc <- forecast(fit, h = 12, method = "ridge2f")
# Forecast 12 periods ahead
# Plot forecasts
plot(fc)

}

}
\references{
Hyndman, R.J., & Shang, H.L. (2009). Forecasting functional time series.
Journal of the Korean Statistical Society, 38(3), 199-221.
}
\seealso{
\code{\link{ftsm}}, \code{\link{plot.ftsf}}

\code{\link{ftsm}}, \code{\link{plot.ftsf}}
}
